#include <stdio.h>
#include <stdlib.h>
#define TAM 20
int main()
{
    int v[TAM], i, maior;
    
    
    
    
   for(i=0; i<TAM ;i++){ 
     v[i] = rand()%20;
     printf("%d ", v[i]);
   }
   
    maior = v[0];
   for(i=0; i<20; i++){
       if(v[i] > maior){
        maior=v[i];}
   }
   
   printf("\nO maior elemento e: %d\n", maior);
    
   return 0; 
}
